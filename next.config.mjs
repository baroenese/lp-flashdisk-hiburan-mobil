/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: {
        unoptimized: true,
    },
    distDir: "website",
    basePath: "/website-flashdisk-hiburan-mobil"
};

export default nextConfig;
